.idea/
.vscode/
.cursor/
run/
run-bench/
build/
.gradle/
src/main/resources/git.properties
benchmark/worlds/
benchmark/results/
benchmark/replays/
remeasure.ps1

# A dev client launched from the project root writes its game directory here. benchmark/run-dev.ps1
# no longer does that, but a stray launch or an IDE run config still can.
/logs/
/saves/
/config/
/crash-reports/
/.mixin.out/
/options.txt
/usercache.json
/usernamecache.json
/FPSMaster Edge/

# SMTC 原生库由 CI 构建后注入（native/smtc/CMakeLists.txt），不提交二进制
src/main/resources/native/windows/*/*.dll
native/smtc/build-*/

# Local OptiFine jars used for Forge-free / AOT smoke runs (do not redistribute).
runtime/libs/
poc/libs/

# Screenshot-pipeline game directory (benchmark/uishot.sh)
run-shots/
# Local seed copy of the developer's server list — never publish
benchmark/shot-seed/servers.dat
.DS_Store
